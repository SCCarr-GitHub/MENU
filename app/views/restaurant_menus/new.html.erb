<div class="container">
  <%= form_with(model: [@restaurant, @restaurant_menu], data: { controller: "ocr" }) do |form| %>
  <!--  <div>-->
      <%= form.label :description %>
      <%= form.text_field :description %>
    <div>
      <%= form.label :photos %>
      <%= form.file_field :photos, data: {  ocr_target: "file", action: "change->ocr#upload" } %>
    </div>
    <%# Hidden input for the photo description %>
    <%#= #form.hidden_field :photo_description, data: { ocr_target: “photo_description” } %>
    <div>
      <%= form.label :photo_description %>
      <%= form.text_area :photo_description, data: { ocr_target: "photo_description" } %>
    </div>
    <div>
      <%= form.submit %>
    </div>
  <% end %>
</div>
